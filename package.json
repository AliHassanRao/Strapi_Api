{
  "name": "strapi_api",
  "version": "1.0.0",
  "description": "Strapi_Api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run start-strapi\" \"npm run start-frontend\"",
    "start-strapi": "cd strapi-api && npm run develop",
    "start-frontend": "cd frontend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
